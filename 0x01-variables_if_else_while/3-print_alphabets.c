#include <stdio.h>

/**
 *main - Entry point
 *Return: returns 0 if successful
 */

int main(void)
{
char lower_case = [[a..z]];
char upper_case = [[A..Z]];
putchar(lower_case);
putchar(upper_case);
putchar('\n');
return (0);
}
