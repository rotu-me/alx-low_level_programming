#include <stdio.h>

/**
 *main - Entry point
 *Return: returns 0 if successful
 */

int main(void)
{
for (i='a'; i <= 'z'; i++) 
putchar(i);
for (i='A'; i <= 'Z'; i++)
putchar(i);
putchar('\n');
return (0);
}
